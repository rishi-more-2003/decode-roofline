# decode-roofline — pinned dependencies
# ---------------------------------------------------------------------------
# Target environment (measured 2026-05-29, see docs/00_environment.md):
#   Windows 11, Python 3.12.9, RTX 4070 Laptop GPU (8 GB, CC 8.9),
#   driver 581.95, CUDA toolkit 13.2 (nvcc), PyTorch 2.6.0+cu124.
#
# IMPORTANT — install torch FIRST from the CUDA 12.4 index, not plain PyPI,
# so you get the CUDA-enabled wheel that matches this machine:
#
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# (Re-listing torch below pins the version; the index-url above selects the
#  CUDA build. Plain `pip install torch==2.6.0` may pull a CPU-only wheel.)
# ---------------------------------------------------------------------------

# --- core: kernel build + harness + correctness/bench ---
torch==2.6.0
numpy==2.1.3
ninja==1.11.1.3            # required by torch.utils.cpp_extension JIT on Windows
safetensors==0.4.5

# --- model loading (Qwen2.5-1.5B / Llama-3.2-1B default targets) ---
transformers==4.49.0
huggingface-hub==0.27.1
sentencepiece==0.2.0
accelerate==1.3.0

# --- analysis / plots / results ---
pandas==2.2.3
matplotlib==3.9.4

# --- testing ---
pytest==8.3.4

# ---------------------------------------------------------------------------
# Phase 1 ONLY — vLLM (measurement story; not needed for the custom kernel).
# vLLM has no native Windows support; on this machine it must run under WSL2
# (or a Linux box). Left commented so `pip install -r requirements.txt`
# does not break on native Windows. We will resolve the WSL path at Phase 1.
#   vllm==0.8.5
# ---------------------------------------------------------------------------
