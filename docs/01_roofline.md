# 01 — Roofline (RTX 4070 Laptop) & per-kernel placement

> **Status: STUB — filled in Phase 1.** This document is the foundation: it
> proves with profiler data that decode is memory-bandwidth-bound on this GPU.

## The 4070 Laptop roofline

The roofline ceiling is set by **measured** DRAM bandwidth, not the datasheet
(~256 GB/s nominal). The measured peak goes in `docs/00_environment.md` and is
used by `profiling/roofline.py`.

- Peak DRAM bandwidth (measured): **_TODO GB/s_**
- Peak FP16/FP32 compute (for the compute ceiling): _TODO_
- Ridge point (FLOP/byte where the workload stops being memory-bound): _TODO_

![roofline placeholder](../bench/results/roofline.png)
<!-- plot generated by `make roofline`; checked in so README renders w/o a GPU -->

## Per-kernel placement (decode step, batch 1)

Populated from `profiling/ncu_kernels.py` output. For each dominant kernel:
arithmetic intensity (FLOP/byte), achieved DRAM throughput vs peak, and whether
it sits under the memory ceiling.

| Kernel | % of decode time | Arith. intensity (FLOP/B) | Achieved BW (GB/s) | % of peak BW | Bound by |
| --- | --- | --- | --- | --- | --- |
| weight GEMV/GEMM | _TODO_ | _TODO_ | _TODO_ | _TODO_ | _TODO_ |
| attention | _TODO_ | _TODO_ | _TODO_ | _TODO_ | _TODO_ |
| RMSNorm | _TODO_ | _TODO_ | _TODO_ | _TODO_ | _TODO_ |
| sampling/other | _TODO_ | _TODO_ | _TODO_ | _TODO_ | _TODO_ |

## Conclusion (Phase 1 exit criterion)

> _TODO — written claim, e.g.: "Decode-step time is X% memory-bound; the
> dominant memory-bound kernel is the weight GEMV at batch 1, achieving Y% of
> the measured Z GB/s peak."_
